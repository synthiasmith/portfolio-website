import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Synthia Smith | Executive Coach & Transformational Consultant",
  description: "Lead with calm. Perform with clarity. Thrive in change. Executive coaching and organizational transformation consultancy for high-achieving leaders.",
  keywords: ["Executive Coach", "Transformational Consultant", "Synthia Smith", "The Mitra Group", "Performance Neuroscience", "Coaching & Mentoring"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
